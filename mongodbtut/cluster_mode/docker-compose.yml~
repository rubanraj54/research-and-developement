version: '3'

services:
  mongo_node1:
    image: mongo
    ports:
      - 30001:27017
    volumes:
      - $HOME/mongodb/data1:/data/db
    command: mongod --replSet ropod-replica
  mongo_node1:
    image: mongo
    ports:
      - 30001:27017
    volumes:
      - $HOME/mongodb/data1:/data/db
    command: mongod --replSet ropod-replica
  mongo_node1:
    image: mongo
    ports:
      - 30001:27017
    volumes:
      - $HOME/mongodb/data1:/data/db
    command: mongod --replSet ropod-replica
